<g:select
   class="form-control"
   name="${property}"
   from="${attrs.from}"
   multiple="${attrs.multiple ?: false}"
   optionKey="${attrs.optionKey}"
   optionValue="${attrs.optionValue}"
   value="${attrs.value}"/>
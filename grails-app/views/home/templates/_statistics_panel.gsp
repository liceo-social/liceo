<div class="small-box bg-primary">
    <div class="inner">
        <h3>${number ?: '-'}</h3>
        <p>${title ?: '-'}</p>
    </div>
    <div class="icon">
        <i class="fas ${icon}"></i>
    </div>
    <a href="${link}" class="small-box-footer">${linkLabel ?: 'Ir a '} <i class="fas fa-arrow-circle-right"></i></a>
</div>
